# Server Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production

# Application Security
SECRET_KEY=change_this_to_a_secure_random_key_in_production

# Database Configuration
DATABASE_PATH=backend/idp_platform.db

# Optional AI Providers (optional: platform operates with full built-in extraction without them)
OPENAI_API_KEY=
GEMINI_API_KEY=
